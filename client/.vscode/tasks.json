{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run golang binbi",
            "type": "shell",
            "command": "go run main.go",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}